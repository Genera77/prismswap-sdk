# Escrow UI reference app — deferred

The reference app is deferred with `@prismswap/escrow-ui` until its canonical `trade_session` preparation dependency can be extracted without the private legacy ABI.
